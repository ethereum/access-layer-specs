# Moved

This spec now lives at [`specs/5-zk-proof-of-personhood`](../5-zk-proof-of-personhood/README.md).

Spec folders were renamed from a bare number to `number-slug` so that
listings are readable. The spec's number did not change. This file stays
so that links to the old path keep working.
