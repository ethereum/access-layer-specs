# Moved

This spec now lives at [`specs/1-coss`](../1-coss/README.md).

Spec folders were renamed from a bare number to the number plus the spec's
shortname in lowercase, so that listings are readable. The spec's number did
not change. This file stays so that links to the old path keep working.
